function multiplicacao(a: number, b: number): number{
    return a*b
}

console.log(multiplicacao(2,3))

function concat(nome: string): string {
    return `Olá, ${nome}`
}

console.log(concat("João"))